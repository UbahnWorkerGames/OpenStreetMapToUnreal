{
  "name": "uemap",
  "private": true,
  "version": "0.1.43",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:dist": "node scripts/build-dist.js",
    "deploy": "npm run build:dist && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "wrangler": "^4.86.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
